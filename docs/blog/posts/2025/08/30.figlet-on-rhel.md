---
title: 'Figlet on RHEL'
tags:
- linux
---

There is no figlet on rhel, but we can fix that:

```bash
dnf install -y python3-pip
python3 -m pip install --user pyfiglet
python3 -m pyfiglet "$(hostname)"
```